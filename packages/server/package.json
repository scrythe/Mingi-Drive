{
  "name": "backend",
  "description": "client-side/frontend code",
  "scripts": {
    "dev": "vite --host",
    "build": "bunx --bun vite build",
    "serve": "vite preview"
  },
  "type": "module",
  "dependencies": {
    "@neondatabase/serverless": "^0.9.4",
    "dotenv": "^16.4.5",
    "drizzle-orm": "^0.32.2"
  },
  "devDependencies": {
    "drizzle-kit": "^0.23.2"
  }
}
